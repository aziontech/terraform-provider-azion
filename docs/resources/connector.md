---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "azion_connector Resource - terraform-provider-azion"
subcategory: ""
description: |-
  Creates a connector resource. Connectors are polymorphic and support different types (http, storage).
---

# azion_connector (Resource)

Creates a connector resource. Connectors are polymorphic and support different types (http, storage).

## Example Usage

```terraform
# =====================================================
# CONNECTOR RESOURCES
# =====================================================

# Storage Connector Example
# Used to connect to object storage services
# NOTE: The bucket name must be a valid, existing bucket
resource "azion_connector" "storage_connector" {
  connector {
    name   = "My Storage Connector"
    type   = "storage"
    active = true
    storage_attributes {
      # Replace with a valid bucket name
      bucket = "my-bucket"
      prefix = "path/to/files/"
    }
  }
}

# HTTP Connector Example
# Used to connect to HTTP origins
resource "azion_connector" "http_connector" {
  connector {
    name   = "My HTTP Connector"
    type   = "http"
    active = true
    http_attributes {
      addresses {
        endpoint {
          address   = "192.168.1.100"
          http_port = 80
          active    = true
        }
      }
    }
  }
}

# HTTP Connector with all options
resource "azion_connector" "http_connector_full" {
  connector {
    name   = "My HTTP Connector Full"
    type   = "http"
    active = true
    http_attributes {
      addresses {
        endpoint {
          address    = "192.168.1.100"
          http_port  = 80
          https_port = 443
          active     = true
          modules {
            load_balancer {
              server_role = "primary"
              weight      = 1
            }
          }
        }
      }
      connection_options {
        dns_resolution      = "both"
        following_redirect  = false
        host                = "$${host}"
        http_version_policy = "http1_1"
        path_prefix         = ""
        real_ip_header      = "X-Real-IP"
        real_port_header    = "X-Real-PORT"
        transport_policy    = "preserve"
      }
      modules {
        load_balancer {
          enabled = true
          config {
            method             = "round_robin"
            max_retries        = 3
            connection_timeout = 60
            read_write_timeout = 120
          }
        }
        origin_shield {
          enabled = false
        }
      }
    }
  }
}

# HTTP Connector with HMAC Origin Shield (S3-compatible)
# Used to connect to S3-compatible origins with HMAC authentication
resource "azion_connector" "s3_with_hmac" {
  connector {
    name   = "S3 Connector with HMAC"
    type   = "http"
    active = true
    http_attributes {
      addresses {
        endpoint {
          address    = "my-bucket.s3.us-east-1.amazonaws.com"
          http_port  = 80
          https_port = 443
          active     = true
        }
      }
      connection_options {
        host             = "my-bucket.s3.amazonaws.com"
        transport_policy = "force_https"
      }
      modules {
        origin_shield {
          enabled = true
          config {
            hmac {
              enabled = true
              config {
                type = "aws4_hmac_sha256"
                attributes {
                  region     = "us-east-1"
                  service    = "s3"
                  access_key = "YOUR_ACCESS_KEY"
                  secret_key = "YOUR_SECRET_KEY"
                }
              }
            }
          }
        }
      }
    }
  }
}

# =====================================================
# DATA SOURCES
# =====================================================

# Read a single connector by ID
data "azion_connector" "by_id" {
  id = azion_connector.http_connector.connector.id
}

# List all connectors in the account
data "azion_connectors" "all" {}

# =====================================================
# OUTPUTS
# =====================================================

output "storage_connector_id" {
  description = "ID of the storage connector"
  value       = azion_connector.storage_connector.connector.id
}

output "storage_connector_name" {
  description = "Name of the storage connector"
  value       = azion_connector.storage_connector.connector.name
}

output "storage_connector_type" {
  description = "Type of the storage connector"
  value       = azion_connector.storage_connector.connector.type
}

output "storage_connector_attributes" {
  description = "Attributes of the storage connector"
  value       = azion_connector.storage_connector.connector.storage_attributes
}

output "http_connector_id" {
  description = "ID of the HTTP connector"
  value       = azion_connector.http_connector.connector.id
}

output "http_connector_name" {
  description = "Name of the HTTP connector"
  value       = azion_connector.http_connector.connector.name
}

output "http_connector_attributes" {
  description = "Attributes of the HTTP connector (includes API defaults)"
  value       = azion_connector.http_connector.connector.http_attributes
}

output "connector_by_id_data" {
  description = "Data from the connector read by ID"
  value       = data.azion_connector.by_id.data
}

output "all_connectors_count" {
  description = "Total count of connectors"
  value       = data.azion_connectors.all.counter
}

output "all_connectors_names" {
  description = "Names of all connectors"
  value       = [for c in data.azion_connectors.all.results : c.name]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `connector` (Block, Optional) (see [below for nested schema](#nestedblock--connector))

### Read-Only

- `id` (String) The ID of this resource.
- `last_updated` (String) Timestamp of the last Terraform update of the resource.
- `schema_version` (Number)

<a id="nestedblock--connector"></a>
### Nested Schema for `connector`

Required:

- `name` (String) Name of the connector.
- `type` (String) Type of the connector (http or storage).

Optional:

- `active` (Boolean) Status of the connector.
- `http_attributes` (Block, Optional) Attributes for HTTP type connectors. Required when type is 'http'. (see [below for nested schema](#nestedblock--connector--http_attributes))
- `storage_attributes` (Block, Optional) Attributes for storage type connectors. Required when type is 'storage'. (see [below for nested schema](#nestedblock--connector--storage_attributes))

Read-Only:

- `created_at` (String) The creation timestamp of the connector.
- `id` (Number) The connector identifier.
- `last_editor` (String) The last editor of the connector.
- `last_modified` (String) Last modified timestamp of the connector.
- `product_version` (String) Product version of the connector.
- `state` (String) The state of the current connector version.
- `version_id` (String) The identifier of the current connector version.

<a id="nestedblock--connector--http_attributes"></a>
### Nested Schema for `connector.http_attributes`

Optional:

- `addresses` (Block List) List of origin endpoints. (see [below for nested schema](#nestedblock--connector--http_attributes--addresses))
- `connection_options` (Block, Optional) HTTP connection options. (see [below for nested schema](#nestedblock--connector--http_attributes--connection_options))
- `modules` (Block, Optional) HTTP modules configuration. (see [below for nested schema](#nestedblock--connector--http_attributes--modules))

<a id="nestedblock--connector--http_attributes--addresses"></a>
### Nested Schema for `connector.http_attributes.addresses`

Optional:

- `endpoint` (Block, Optional) A single origin endpoint configuration. (see [below for nested schema](#nestedblock--connector--http_attributes--addresses--endpoint))

<a id="nestedblock--connector--http_attributes--addresses--endpoint"></a>
### Nested Schema for `connector.http_attributes.addresses.endpoint`

Required:

- `address` (String) The origin address (IP or hostname).

Optional:

- `active` (Boolean) Whether the address is active.
- `http_port` (Number) HTTP port number.
- `https_port` (Number) HTTPS port number.
- `modules` (Block, Optional) Address-level modules. (see [below for nested schema](#nestedblock--connector--http_attributes--addresses--endpoint--modules))

<a id="nestedblock--connector--http_attributes--addresses--endpoint--modules"></a>
### Nested Schema for `connector.http_attributes.addresses.endpoint.modules`

Optional:

- `load_balancer` (Block, Optional) Load balancer module at address level. (see [below for nested schema](#nestedblock--connector--http_attributes--addresses--endpoint--modules--load_balancer))

<a id="nestedblock--connector--http_attributes--addresses--endpoint--modules--load_balancer"></a>
### Nested Schema for `connector.http_attributes.addresses.endpoint.modules.load_balancer`

Optional:

- `server_role` (String) Role of the address in load balancing (primary or backup).
- `weight` (Number) Weight used in load balancing strategy.





<a id="nestedblock--connector--http_attributes--connection_options"></a>
### Nested Schema for `connector.http_attributes.connection_options`

Optional:

- `dns_resolution` (String) DNS resolution strategy.
- `following_redirect` (Boolean) Whether to follow redirects.
- `host` (String) Host header value. Use ${host} to pass through the original host.
- `http_version_policy` (String) HTTP version policy.
- `path_prefix` (String) Path prefix for requests.
- `real_ip_header` (String) Header for real IP.
- `real_port_header` (String) Header for real port.
- `transport_policy` (String) Transport policy.


<a id="nestedblock--connector--http_attributes--modules"></a>
### Nested Schema for `connector.http_attributes.modules`

Optional:

- `load_balancer` (Block, Optional) Load balancer module. (see [below for nested schema](#nestedblock--connector--http_attributes--modules--load_balancer))
- `origin_shield` (Block, Optional) Origin shield module. (see [below for nested schema](#nestedblock--connector--http_attributes--modules--origin_shield))

<a id="nestedblock--connector--http_attributes--modules--load_balancer"></a>
### Nested Schema for `connector.http_attributes.modules.load_balancer`

Optional:

- `config` (Block, Optional) Load balancer configuration. (see [below for nested schema](#nestedblock--connector--http_attributes--modules--load_balancer--config))
- `enabled` (Boolean) Whether load balancer is enabled.

<a id="nestedblock--connector--http_attributes--modules--load_balancer--config"></a>
### Nested Schema for `connector.http_attributes.modules.load_balancer.config`

Optional:

- `connection_timeout` (Number) Maximum time (in seconds) to wait for a connection to be established.
- `max_retries` (Number) Maximum number of retry attempts on connection failure.
- `method` (String) Load balancing method (round_robin, least_conn, ip_hash).
- `read_write_timeout` (Number) Maximum time (in seconds) to wait for data read/write after connection.



<a id="nestedblock--connector--http_attributes--modules--origin_shield"></a>
### Nested Schema for `connector.http_attributes.modules.origin_shield`

Optional:

- `config` (Block, Optional) Origin shield configuration. (see [below for nested schema](#nestedblock--connector--http_attributes--modules--origin_shield--config))
- `enabled` (Boolean) Whether origin shield is enabled.

<a id="nestedblock--connector--http_attributes--modules--origin_shield--config"></a>
### Nested Schema for `connector.http_attributes.modules.origin_shield.config`

Optional:

- `hmac` (Block, Optional) HMAC configuration for origin shield. (see [below for nested schema](#nestedblock--connector--http_attributes--modules--origin_shield--config--hmac))
- `origin_ip_acl` (Block, Optional) Origin IP ACL configuration. (see [below for nested schema](#nestedblock--connector--http_attributes--modules--origin_shield--config--origin_ip_acl))

<a id="nestedblock--connector--http_attributes--modules--origin_shield--config--hmac"></a>
### Nested Schema for `connector.http_attributes.modules.origin_shield.config.hmac`

Optional:

- `config` (Block, Optional) AWS4 HMAC configuration. (see [below for nested schema](#nestedblock--connector--http_attributes--modules--origin_shield--config--hmac--config))
- `enabled` (Boolean) Whether HMAC is enabled.

<a id="nestedblock--connector--http_attributes--modules--origin_shield--config--hmac--config"></a>
### Nested Schema for `connector.http_attributes.modules.origin_shield.config.hmac.config`

Optional:

- `attributes` (Block, Optional) AWS4 HMAC attributes. (see [below for nested schema](#nestedblock--connector--http_attributes--modules--origin_shield--config--hmac--config--attributes))
- `type` (String) HMAC type (e.g., aws4_hmac_sha256).

<a id="nestedblock--connector--http_attributes--modules--origin_shield--config--hmac--config--attributes"></a>
### Nested Schema for `connector.http_attributes.modules.origin_shield.config.hmac.config.attributes`

Optional:

- `access_key` (String, Sensitive) AWS access key.
- `region` (String) AWS region.
- `secret_key` (String, Sensitive) AWS secret key.
- `service` (String) AWS service name.




<a id="nestedblock--connector--http_attributes--modules--origin_shield--config--origin_ip_acl"></a>
### Nested Schema for `connector.http_attributes.modules.origin_shield.config.origin_ip_acl`

Optional:

- `enabled` (Boolean) Whether the origin IP ACL is enabled.






<a id="nestedblock--connector--storage_attributes"></a>
### Nested Schema for `connector.storage_attributes`

Optional:

- `bucket` (String) The name of the bucket.
- `prefix` (String) The prefix path within the bucket.

## Import

Import is supported using the following syntax:

```shell
# Import a connector by its ID
terraform import azion_connector.example connector_id
```
