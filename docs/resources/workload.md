---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "azion_workload Resource - terraform-provider-azion"
subcategory: ""
description: |-
  Resource for managing Azion Workloads.
---

# azion_workload (Resource)

Resource for managing Azion Workloads.

## Example Usage

```terraform
resource "azion_workload" "example" {
  workload {
    name           = "My Workload"
    active         = true
    infrastructure = 1

    tls {
      certificate     = 1234
      ciphers         = 3
      minimum_version = "tls_1_2"
    }

    protocols {
      http {
        versions    = ["http1", "http2", "http3"]
        http_ports  = [80]
        https_ports = [443]
        quic_ports  = [443]
      }
    }

    mtls {
      enabled = true
      config {
        certificate  = 5678
        crl          = [9012]
        verification = "enforce"
      }
    }

    domains                      = ["example.com", "www.example.com"]
    workload_domain_allow_access = true
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `workload` (Block, Optional) (see [below for nested schema](#nestedblock--workload))

### Read-Only

- `id` (String) The ID of this resource.
- `last_updated` (String) Timestamp of the last Terraform update of the resource.

<a id="nestedblock--workload"></a>
### Nested Schema for `workload`

Required:

- `name` (String) Name of the workload.

Optional:

- `active` (Boolean) Status of the workload.
- `domains` (Set of String) Set of domains associated with the workload.
- `infrastructure` (Number) Infrastructure type: 1 for Production (All Locations), 2 for Staging.
- `mtls` (Block, Optional) Mutual TLS configuration for the workload. (see [below for nested schema](#nestedblock--workload--mtls))
- `protocols` (Block, Optional) Protocol configurations for the workload. (see [below for nested schema](#nestedblock--workload--protocols))
- `tls` (Block, Optional) TLS configuration for the workload. (see [below for nested schema](#nestedblock--workload--tls))
- `workload_domain_allow_access` (Boolean) Whether domain access is allowed.

Read-Only:

- `created_at` (String) Creation timestamp of the workload.
- `id` (Number) The workload identifier.
- `last_editor` (String) The last editor of the workload.
- `last_modified` (String) Last modified timestamp of the workload.
- `product_version` (String) Product version of the workload.
- `workload_domain` (String) The workload domain.

<a id="nestedblock--workload--mtls"></a>
### Nested Schema for `workload.mtls`

Optional:

- `config` (Block, Optional) MTLS configuration. (see [below for nested schema](#nestedblock--workload--mtls--config))
- `enabled` (Boolean) Whether MTLS is enabled.

<a id="nestedblock--workload--mtls--config"></a>
### Nested Schema for `workload.mtls.config`

Optional:

- `certificate` (Number) MTLS certificate ID.
- `crl` (List of Number) Certificate Revocation List.
- `verification` (String) MTLS verification type: enforce or permissive.



<a id="nestedblock--workload--protocols"></a>
### Nested Schema for `workload.protocols`

Optional:

- `http` (Block, Optional) HTTP protocol configuration. (see [below for nested schema](#nestedblock--workload--protocols--http))

<a id="nestedblock--workload--protocols--http"></a>
### Nested Schema for `workload.protocols.http`

Optional:

- `http_ports` (List of Number) HTTP ports.
- `https_ports` (List of Number) HTTPS ports.
- `quic_ports` (List of Number) QUIC ports. When omitted, the value is determined by the API (QUIC is only required when http3 is present in versions).
- `versions` (List of String) HTTP versions supported.



<a id="nestedblock--workload--tls"></a>
### Nested Schema for `workload.tls`

Optional:

- `certificate` (Number) Certificate ID for TLS.
- `ciphers` (Number) Cipher suite configuration.
- `minimum_version` (String) Minimum TLS version.

## Import

Import is supported using the following syntax:

```shell
terraform import azion_workload.example 123456
```
