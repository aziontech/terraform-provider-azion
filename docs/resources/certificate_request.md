---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "azion_certificate_request Resource - terraform-provider-azion"
subcategory: ""
description: |-
  Provides a certificate request resource for Let's Encrypt certificates. This resource allows you to request SSL/TLS certificates from Let's Encrypt automatically.
  ~> Note: This resource only supports creation. Update operations are not available. Read and Delete operations use the standard digital certificates endpoint.
  ~> Note about challenge types:
  Use dns challenge for DNS-based validation or http challenge for HTTP-based validation. The challenge type determines how Let's Encrypt will verify domain ownership.
---

# azion_certificate_request (Resource)

Provides a certificate request resource for Let's Encrypt certificates. This resource allows you to request SSL/TLS certificates from Let's Encrypt automatically.

~> **Note:** This resource only supports creation. Update operations are not available. Read and Delete operations use the standard digital certificates endpoint.

~> **Note about challenge types:**
Use `dns` challenge for DNS-based validation or `http` challenge for HTTP-based validation. The challenge type determines how Let's Encrypt will verify domain ownership.

## Example Usage

```terraform
resource "azion_certificate_request" "example" {
  results {
    name        = "my-letsencrypt-certificate"
    common_name = "example.com"
    challenge   = "dns"
    authority   = "lets_encrypt"

    alternative_names = [
      "www.example.com",
      "api.example.com"
    ]
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `results` (Block, Optional) The certificate request details. (see [below for nested schema](#nestedblock--results))

### Read-Only

- `id` (String) Identifier of the resource.
- `last_updated` (String) Timestamp of the last Terraform update of the resource.
- `schema_version` (Number) Schema version of the resource.

<a id="nestedblock--results"></a>
### Nested Schema for `results`

Required:

- `authority` (String) Certificate authority. Options: `lets_encrypt`.
- `challenge` (String) Challenge type for ACME certificate validation. Options: `dns` (Uses DNS to solve the ACME challenge), `http` (Uses HTTP to solve the ACME challenge).
- `common_name` (String) Common Name (CN) for the certificate. This is the primary domain name.
- `name` (String) Name of the certificate.

Optional:

- `alternative_names` (List of String) Subject Alternative Names (SANs) for the certificate. Additional domain names to include.
- `key_algorithm` (String) Key algorithm used for the certificate. Options: `rsa_2048` (2048-bit RSA), `rsa_4096` (4096-bit RSA), `ecc_384` (384-bit Prime Field Curve).

Read-Only:

- `active` (Boolean) Whether the certificate is active.
- `certificate_content` (String, Sensitive) The content of the certificate (PEM format). This field is populated after the certificate is issued.
- `certificate_type` (String) Type of the certificate.
- `created_at` (String) Creation timestamp of the certificate.
- `csr` (String) Certificate Signing Request (CSR).
- `id` (Number) Identifier of the certificate.
- `issuer` (String) Issuer of the certificate.
- `last_editor` (String) Last editor of the certificate.
- `last_modified` (String) Last modified timestamp of the certificate.
- `managed` (Boolean) Whether the certificate is managed by Azion.
- `private_key` (String, Sensitive) Private key of the certificate (PEM format). This field is populated after the certificate is issued.
- `product_version` (String) Product version of the certificate.
- `renewed_at` (String) Renewal timestamp of the certificate.
- `status` (String) Status of the certificate. Options: `pending`, `challenge_verification`, `active`, `inactive`, `expired`, `failed`.
- `status_detail` (String) Status detail of the certificate.
- `subject_name` (List of String) Subject name of the certificate.
- `validity` (String) Validity of the certificate.
